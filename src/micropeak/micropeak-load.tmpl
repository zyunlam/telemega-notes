#!/bin/sh
dir=`dirname $0`

HEX="$dir"/"%HEX%"
LOADCMD="%LOADCMD%"
LOADARG="%LOADARG%"
LOADSLOW="%LOADSLOW%"
LOADFAST=""

case "$1" in
fast)
	LOADSPEED="$LOADFAST"
	;;
*)
	LOADSPEED="$LOADSLOW"
	;;
esac

echo ${LOADCMD} ${LOADSPEED} ${LOADARG}${HEX}
${LOADCMD} ${LOADSPEED} ${LOADARG}${HEX}
